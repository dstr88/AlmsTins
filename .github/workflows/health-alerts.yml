name: Health Factor Alerts

on:
  schedule:
    # Run every 30 minutes
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  health-alerts:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger health factor alert check
        env:
          SYNC_URL:    ${{ secrets.SYNC_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          if [ -z "$SYNC_URL" ]; then
            echo "SYNC_URL secret missing."
            exit 1
          fi
          curl -sS --fail --retry 4 --retry-all-errors --retry-delay 15 --retry-max-time 120 --connect-timeout 20 --max-time 120 \
            "$SYNC_URL/api/cron/health-alerts?secret=$CRON_SECRET" \
            -H "x-cron-secret: $CRON_SECRET"
