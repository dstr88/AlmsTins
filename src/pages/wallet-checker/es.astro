---
import LoginLayout from '../../layouts/LoginLayout.astro';
import WalletChecker from '../../components/WalletChecker';

// Si ?address= está en la URL, pre-rellenar el verificador (permite compartir resultados)
const prefilledAddress = Astro.url.searchParams.get('address') ?? '';
---

<LoginLayout
  title="Verificador de Estafas de Cripto — ¿Es Segura Esta Dirección?"
  description="Pega cualquier dirección de billetera de Ethereum, Solana o Sui para verificar al instante si hay estafas conocidas, phishing, honeypots, actividad en la dark web y uso de mixers. Gratis, sin cuenta requerida."
  noindex={false}
>

  <!-- JSON-LD: esquema WebApplication para resultados enriquecidos -->
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "WebApplication",
    "name": "Verificador de Estafas de Cripto",
    "url": "https://almstins.com/wallet-checker/es",
    "description": "Herramienta gratuita para verificar cualquier dirección de billetera cripto en busca de estafas conocidas, phishing, honeypots, actividad en la dark web y uso de mixers. Compatible con Ethereum, Solana y Sui.",
    "applicationCategory": "FinanceApplication",
    "operatingSystem": "Any",
    "browserRequirements": "Requires JavaScript",
    "offers": {
      "@type": "Offer",
      "price": "0",
      "priceCurrency": "USD"
    },
    "creator": {
      "@type": "Organization",
      "name": "Almstins",
      "url": "https://almstins.com"
    },
    "featureList": [
      "Detección de direcciones de estafa conocidas",
      "Identificación de billeteras de phishing",
      "Detección de tokens honeypot",
      "Marcado de transacciones en la dark web",
      "Detección de Mixer / Tornado Cash",
      "Identificación de contratos multi-sig",
      "Consulta de saldo de tokens",
      "Historial de actividad y antigüedad de la billetera"
    ]
  })} />

  <!-- Barra de navegación superior -->
  <nav class="topnav" aria-label="Navegación del sitio Almstins">
    <a class="topnav__brand" href="/" aria-label="Inicio de Almstins">
      <span class="topnav__wordmark">Almstins</span>
      <span class="topnav__tagline">Rastreador de portafolio cripto y herramienta de contabilidad</span>
    </a>
    <div class="topnav__actions">
      <a class="topnav__lang" href="/wallet-checker" aria-label="View in English">🇺🇸 In English</a>
      <a class="topnav__login" href="/login">Iniciar sesión</a>
      <a class="topnav__signup" href="/login">Registrarse gratis</a>
    </div>
  </nav>

  <div class="page">

    <!-- Hero -->
    <header class="hero">
      <div class="hero__eyebrow">Gratis · Sin cuenta · Resultados en segundos</div>
      <h1 class="hero__title">Verificador de Estafas de Cripto</h1>
      <p class="hero__sub">
        ¿Te ofrecieron un trato que parece demasiado bueno para ser verdad? Antes de enviar un solo token,
        pega la dirección de la billetera aquí abajo. La verificaremos contra bases de datos públicas de estafas,
        detectores de honeypots y datos on-chain — al instante.
      </p>

      <!-- Cuadrícula de tres amenazas -->
      <div class="threat-grid">

        <!-- Tarjeta 1: Sitio malicioso -->
        <div class="threat-card">
          <button class="threat-card__top" data-modal="modal-phishing" aria-haspopup="dialog">
            <span class="threat-card__icon">🌐</span>
            <span class="threat-card__title">Conexión a sitio malicioso</span>
            <span class="threat-card__cta">¿Qué es esto? →</span>
          </button>
          <div class="threat-card__divider"></div>
          <div class="threat-card__checker">
            <input
              class="threat-card__input"
              type="text"
              placeholder="Pega una URL o dominio…"
              autocomplete="off"
              spellcheck="false"
              data-checker="dapp"
            />
            <button class="threat-card__check-btn" data-checker="dapp">Verificar sitio</button>
          </div>
        </div>

        <!-- Tarjeta 2: Estafa conocida -->
        <div class="threat-card">
          <button class="threat-card__top" data-modal="modal-scam" aria-haspopup="dialog">
            <span class="threat-card__icon">💀</span>
            <span class="threat-card__title">Invertir en una estafa conocida</span>
            <span class="threat-card__cta">¿Qué es esto? →</span>
          </button>
          <div class="threat-card__divider"></div>
          <div class="threat-card__checker">
            <input
              class="threat-card__input"
              type="text"
              placeholder="Pega una dirección de billetera…"
              autocomplete="off"
              spellcheck="false"
              data-checker="wallet"
            />
            <button class="threat-card__check-btn" data-checker="wallet">Verificar billetera</button>
          </div>
        </div>

        <!-- Tarjeta 3: Robo por aprobación de tokens -->
        <div class="threat-card">
          <button class="threat-card__top" data-modal="modal-approval" aria-haspopup="dialog">
            <span class="threat-card__icon">🔓</span>
            <span class="threat-card__title">Robo por aprobación de tokens</span>
            <span class="threat-card__cta">¿Qué es esto? →</span>
          </button>
          <div class="threat-card__divider"></div>
          <div class="threat-card__approval-links">
            <p class="threat-card__approval-desc">Estas herramientas de la comunidad te muestran al instante qué contratos tienen acceso a tu cripto — y te permiten revocarlos.</p>
            <a href="https://revoke.cash" target="_blank" rel="noopener noreferrer" class="threat-card__approval-link">
              <span class="approval-link__name">revoke.cash</span>
              <span class="approval-link__arrow">↗</span>
            </a>
            <a href="https://debank.com" target="_blank" rel="noopener noreferrer" class="threat-card__approval-link">
              <span class="approval-link__name">debank.com</span>
              <span class="approval-link__arrow">↗</span>
            </a>
            <a href="https://unrekt.me" target="_blank" rel="noopener noreferrer" class="threat-card__approval-link">
              <span class="approval-link__name">unrekt.me</span>
              <span class="approval-link__arrow">↗</span>
            </a>
          </div>
        </div>

      </div>
    </header>

    <!-- Modal: Sitio malicioso / phishing -->
    <div id="modal-phishing" class="threat-modal" role="dialog" aria-modal="true" aria-labelledby="modal-phishing-title">
      <div class="threat-modal__box">
        <button class="threat-modal__close" aria-label="Cerrar">&times;</button>
        <div class="threat-modal__icon">🌐</div>
        <h2 class="threat-modal__title" id="modal-phishing-title">Conectarse a un sitio malicioso</h2>
        <p class="threat-modal__subtitle">Wallet drainers y dApps de phishing</p>
        <div class="threat-modal__body">
          <p>Los estafadores crean copias casi perfectas de dApps legítimas — mints falsos de NFTs, reclamaciones falsas de tokens, páginas falsas de airdrop. El sitio parece real. La URL es parecida pero ligeramente diferente (<code>blur-io.xyz</code> en lugar de <code>blur.io</code>). Cuando conectas tu MetaMask y firmas lo que parece una transacción rutinaria, en realidad estás otorgando permiso para vaciar todos los tokens de tu billetera en un solo movimiento.</p>
          <div class="threat-modal__example">
            💸 <strong>Ejemplo real:</strong> En 2023, una página falsa de airdrop de Blur.io drenó más de <strong>$1.2 millones</strong> de titulares de NFTs en pocas horas tras su lanzamiento. La mayoría de las víctimas dijeron que "verificaron la URL dos veces" pero no vieron un solo carácter diferente.
          </div>
          <p>Estos sitios aparecen como enlaces en mensajes directos de Discord, respuestas en Twitter/X, grupos de Telegram e incluso anuncios pagados. El atacante paga por un anuncio de Google para aparecer encima del sitio real.</p>
          <div class="threat-modal__tip">
            ✅ <strong>Cómo te protege nuestro verificador de dApps:</strong> Pega la URL antes de conectarte. Consultamos 7 bases de datos de seguridad — incluyendo la propia lista de bloqueo de phishing de MetaMask y la base de datos de 345,000 dominios de ScamSniffer — y devolvemos un veredicto en segundos. <strong>Regla de oro: nunca hagas clic en un enlace de billetera enviado por mensaje directo.</strong>
          </div>
        </div>
      </div>
    </div>

    <!-- Modal: Inversión en estafa conocida -->
    <div id="modal-scam" class="threat-modal" role="dialog" aria-modal="true" aria-labelledby="modal-scam-title">
      <div class="threat-modal__box">
        <button class="threat-modal__close" aria-label="Cerrar">&times;</button>
        <div class="threat-modal__icon">💀</div>
        <h2 class="threat-modal__title" id="modal-scam-title">Invertir en una estafa conocida</h2>
        <p class="threat-modal__subtitle">Pig butchering, honeypots y rug pulls</p>
        <div class="threat-modal__body">
          <p>Las estafas de <strong>pig butchering</strong> construyen confianza durante semanas o meses — un nuevo contacto en una app de citas o redes sociales te va presentando gradualmente una "gran oportunidad de inversión". La dirección de billetera que te envían a menudo ya ha sido reportada. Los <strong>tokens honeypot</strong> te permiten comprar pero te bloquean para vender — el contrato está programado para atrapar tus fondos mientras el desarrollador vacía el pool de liquidez.</p>
          <div class="threat-modal__example">
            💸 <strong>Ejemplo real:</strong> El Centro de Quejas de Crímenes en Internet (IC3) del FBI reportó <strong>$3.3 mil millones</strong> en fraudes de inversión cripto solo en 2023 — la categoría de fraude de más rápido crecimiento. La mayoría de las víctimas nunca habían escuchado sobre el "pig butchering" antes de sufrirlo.
          </div>
          <p>Un rug pull parece un lanzamiento de token legítimo. Los desarrolladores generan hype del proyecto, entra liquidez, luego el equipo retira todo de la noche a la mañana y el token cae a cero.</p>
          <div class="threat-modal__tip">
            ✅ <strong>Cómo te protege nuestro verificador de billeteras:</strong> Antes de enviar fondos a cualquier billetera, pega la dirección aquí. La verificamos contra la lista negra global de estafas de GoPlus Security, buscamos patrones honeypot en los tokens asociados y señalamos el uso de mixers que indica lavado de dinero. Si alguien te presiona para enviar cripto rápidamente — eso es la estafa.
          </div>
        </div>
      </div>
    </div>

    <!-- Modal: Robo por aprobación de tokens -->
    <div id="modal-approval" class="threat-modal" role="dialog" aria-modal="true" aria-labelledby="modal-approval-title">
      <div class="threat-modal__box">
        <button class="threat-modal__close" aria-label="Cerrar">&times;</button>
        <div class="threat-modal__icon">🔓</div>
        <h2 class="threat-modal__title" id="modal-approval-title">Robo por aprobación de tokens</h2>
        <p class="threat-modal__subtitle">Aprobaciones ilimitadas y vaciados silenciosos</p>
        <div class="threat-modal__body">
          <p>Cada vez que haces clic en "Aprobar" en un swap de tokens o un mint de NFT, firmas un mensaje de contrato inteligente que dice <em>"este contrato puede gastar mis tokens"</em>. La mayoría de las dApps usan <strong>aprobación ilimitada</strong> por defecto — pueden tomar todo tu saldo de ese token, en cualquier momento, para siempre, hasta que lo revoques explícitamente. Estos permisos persisten mucho después de que hayas olvidado que el sitio existió.</p>
          <div class="threat-modal__example">
            💸 <strong>Ejemplo real:</strong> Cuando el puente Multichain fue explotado en 2023, los atacantes usaron <strong>aprobaciones antiguas</strong> que los usuarios habían otorgado meses antes para drenar <strong>$125 millones</strong>. Muchas víctimas no habían usado el puente en más de un año.
          </div>
          <p>Una dApp comprometida, un rug pull, o un exploit de día cero puede activar esas aprobaciones en el momento en que se lanza — sin necesidad de una segunda firma de tu parte.</p>
          <div class="threat-modal__tip">
            ✅ <strong>Cómo protegerte:</strong> Visita <a href="https://revoke.cash" target="_blank" rel="noopener noreferrer" style="color:#a5b4fc">revoke.cash</a> para ver cada aprobación activa en tu billetera y revocar todo lo que no reconozcas. Siempre establece montos exactos en lugar de ilimitados cuando tu billetera te dé esa opción — y revoca después de cada interacción que hayas terminado.
          </div>
        </div>
      </div>
    </div>

    <!-- Selector de pestañas -->
    <div class="checker-tabs" role="tablist" aria-label="Tipo de verificación">
      <button class="checker-tab active" id="tab-btn-wallet" role="tab" aria-selected="true"  aria-controls="panel-wallet" data-tab="wallet">
        🔍 Dirección de Billetera
      </button>
      <button class="checker-tab"        id="tab-btn-dapp"   role="tab" aria-selected="false" aria-controls="panel-dapp"   data-tab="dapp">
        🌐 dApp / Sitio Web
      </button>
    </div>

    <!-- Panel del verificador de billeteras -->
    <div id="panel-wallet" class="checker-wrap" role="tabpanel" aria-labelledby="tab-btn-wallet">
      <WalletChecker client:load prefilledAddress={prefilledAddress} />
    </div>

    <!-- Panel del verificador de dApps -->
    <div id="panel-dapp" class="dapp-panel" role="tabpanel" aria-labelledby="tab-btn-dapp" hidden>

      <div class="dapp-input-row">
        <input
          id="dapp-url-input"
          type="text"
          placeholder="https://dapp-sospechosa.xyz  o simplemente pega el dominio"
          autocomplete="off"
          spellcheck="false"
        />
        <button id="dapp-check-btn" type="button">Verificar sitio</button>
      </div>

      <div id="dapp-result" class="dapp-result" hidden>

        <!-- Banner de veredicto -->
        <div class="verdict-banner" id="verdict-banner">
          <span class="verdict-icon" id="verdict-icon"></span>
          <div>
            <p class="verdict-label" id="verdict-label"></p>
            <p class="verdict-url"  id="verdict-url"></p>
          </div>
        </div>

        <!-- Desglose por fuente -->
        <ul class="source-list" id="source-list"></ul>

        <!-- Consejo de seguridad -->
        <div class="safety-tip" id="safety-tip"></div>
      </div>

      <div id="dapp-loading" class="dapp-loading" hidden>
        <span class="spinner"></span> Consultando 7 bases de datos de seguridad…
      </div>

      <div id="dapp-error" class="dapp-error" hidden></div>
    </div>

    <!-- CTA de Almstins -->
    <section class="cta-banner" aria-label="Acerca de Almstins">
      <div class="cta-banner__icon" aria-hidden="true">📊</div>
      <div class="cta-banner__body">
        <p class="cta-banner__headline">¿Quieres rastrear todo lo que tienes?</p>
        <p class="cta-banner__sub">
          Almstins conecta tus billeteras, posiciones DeFi y cuentas de exchanges en un solo lugar —
          y rastrea automáticamente tus ganancias de capital, tenencias y ganancias realizadas.
        </p>
      </div>
      <a class="cta-banner__btn" href="/login">Comenzar gratis →</a>
    </section>

    <!-- Qué verificamos -->
    <section class="signals" aria-labelledby="signals-heading">
      <h2 id="signals-heading" class="signals__title">Qué verificamos</h2>

      <p class="signals__sub">🔍 Verificador de dirección de billetera</p>
      <div class="signals__grid">
        {[
          { icon: '🚨', label: 'Bases de datos de estafas conocidas', body: 'Cruzado contra la lista negra global de GoPlus Security de billeteras de estafa, phishing y vaciado reportadas.' },
          { icon: '🍯', label: 'Detección de honeypot',              body: 'Verifica si los tokens asociados con esta dirección realmente pueden venderse — o si están diseñados para atrapar tus fondos.' },
          { icon: '🌑', label: 'Actividad en la dark web',           body: 'Señala direcciones con conexiones conocidas a mercados de la dark web y patrones de transacciones ilícitas.' },
          { icon: '🔀', label: 'Mixer / Tornado Cash',               body: 'Detecta el uso de mixers cripto como Tornado Cash — una forma común que usan los estafadores para lavar fondos antes de un rug pull.' },
          { icon: '📅', label: 'Antigüedad de la billetera',         body: 'Las billeteras nuevas (< 30 días) son una gran señal de alerta. Los estafadores crean direcciones nuevas para cada operación.' },
          { icon: '💰', label: 'Tenencias de tokens',                body: 'Muestra lo que realmente hay en la billetera. Las billeteras de estafa suelen contener tokens sin valor diseñados para parecer valiosos.' },
          { icon: '⚖️', label: 'Verificación de sanciones',          body: 'Verifica contra las listas de sanciones de la OFAC e internacionales para direcciones involucradas en crímenes financieros.' },
          { icon: '🔑', label: 'Detección multi-sig',                body: 'Identifica si la dirección es un contrato multi-sig. Las inversiones legítimas nunca te piden depositar en los suyos.' },
        ].map(s => (
          <div class="signal-card">
            <span class="signal-card__icon">{s.icon}</span>
            <h3 class="signal-card__label">{s.label}</h3>
            <p class="signal-card__body">{s.body}</p>
          </div>
        ))}
      </div>

      <p class="signals__sub" style="margin-top:2rem">🌐 Verificador de dApp / sitio web</p>
      <div class="signals__grid">
        {[
          { icon: '🦊', label: 'Lista de bloqueo de MetaMask',  body: 'Verifica contra la propia lista eth-phishing-detect de MetaMask — más de 198,000 dominios de phishing cripto mantenidos por el equipo de seguridad de MetaMask.' },
          { icon: '🕵️', label: 'Base de datos de ScamSniffer', body: 'La lista de dominios de phishing web3 más grande disponible, con más de 345,000 sitios reportados. Actualizada diariamente por el equipo de seguridad de ScamSniffer.' },
          { icon: '🛡️', label: 'GoPlus Security',              body: 'Consulta en tiempo real contra la API de phishing web3 en vivo de GoPlus — el mismo motor usado por MetaMask, Trust Wallet y otras billeteras importantes.' },
          { icon: '🔬', label: 'URLScan.io',                    body: 'Busca escaneos previos de investigadores de seguridad del dominio para mostrar cualquier veredicto malicioso de la comunidad global de seguridad.' },
          { icon: '🎣', label: 'Feed de OpenPhish',             body: 'Cruza contra la lista activamente mantenida de OpenPhish de URLs de phishing en vivo actualizada en tiempo real.' },
          { icon: '🔍', label: 'Google Safe Browsing',          body: 'Cuando está configurado, consulta la base de datos de amenazas de Google — uno de los repositorios de URLs de phishing y malware más grandes del mundo.' },
          { icon: '🦠', label: 'VirusTotal',                    body: 'Cuando está configurado, verifica la URL contra más de 70 motores antivirus y de seguridad simultáneamente para un veredicto completo.' },
          { icon: '⚠️', label: 'Atribución, no veredicto',     body: 'Reportamos lo que dicen las bases de datos de terceros. No declaramos de forma independiente que un sitio sea una estafa. Verifica siempre antes de conectar tu billetera.' },
        ].map(s => (
          <div class="signal-card">
            <span class="signal-card__icon">{s.icon}</span>
            <h3 class="signal-card__label">{s.label}</h3>
            <p class="signal-card__body">{s.body}</p>
          </div>
        ))}
      </div>
    </section>

    <!-- Sección de aprobaciones / conexiones de billetera -->
    <section class="approvals-section" aria-labelledby="approvals-heading">
      <h2 id="approvals-heading" class="approvals__title">¿Está tu billetera conectada a algo que no debería?</h2>
      <p class="approvals__intro">
        Cada vez que conectas MetaMask (o cualquier billetera) a una dApp y apruebas una transacción, le estás otorgando
        a ese contrato permiso para mover tokens en tu nombre — a veces sin límite de gasto y sin fecha de vencimiento.
        Estas aprobaciones permanecen activas incluso después de que dejas de usar el sitio. Una dApp comprometida o maliciosa puede
        vaciar tu billetera meses después usando un permiso que olvidaste que otorgaste.
      </p>

      <div class="approvals__cards">
        <div class="approvals__card approvals__card--what">
          <h3>⚠️ Lo que una aprobación realmente significa</h3>
          <p>
            Cuando haces clic en "Aprobar" en un swap de tokens o un mint de NFT, estás firmando una llamada a contrato inteligente que dice
            <em>"este contrato puede gastar X cantidad de mis tokens"</em>. Muchas dApps usan
            <strong>aprobación ilimitada</strong> por defecto — lo que significa que pueden tomar todo lo que tienes de ese token, en cualquier momento,
            para siempre, hasta que lo revoques.
          </p>
          <p>
            Si esa dApp es explotada después, hace un rug pull, o resulta haber sido maliciosa desde el principio,
            el atacante puede usar tu aprobación existente para vaciar tu billetera — sin necesidad de una segunda firma.
          </p>
        </div>

        <div class="approvals__card approvals__card--how">
          <h3>🔍 Cómo ver y revocar tus aprobaciones</h3>
          <p>Estas herramientas gratuitas se conectan a tu billetera (solo lectura) y muestran cada aprobación activa en todas las cadenas — luego te permiten revocar las que no reconoces o ya no necesitas.</p>
          <ul class="approvals__tools">
            <li>
              <a href="https://revoke.cash" target="_blank" rel="noopener noreferrer" class="tool-link">
                <span class="tool-link__name">revoke.cash</span>
                <span class="tool-link__badge">Más confiable</span>
              </a>
              <span class="tool-link__desc">El estándar de oro. Multi-cadena, muestra aprobaciones ilimitadas vs. limitadas, revocación con un clic. Sin cuenta necesaria.</span>
            </li>
            <li>
              <a href="https://debank.com" target="_blank" rel="noopener noreferrer" class="tool-link">
                <span class="tool-link__name">debank.com</span>
              </a>
              <span class="tool-link__desc">Vista completa del portafolio más una pestaña de aprobaciones. Ideal si también quieres ver tus posiciones DeFi junto con tu exposición al riesgo.</span>
            </li>
            <li>
              <a href="https://unrekt.me" target="_blank" rel="noopener noreferrer" class="tool-link">
                <span class="tool-link__name">unrekt.me</span>
              </a>
              <span class="tool-link__desc">Simple y rápido. Pega tu dirección (sin necesidad de conectar billetera) para ver aprobaciones en Ethereum y cadenas compatibles con EVM.</span>
            </li>
          </ul>
        </div>
      </div>

      <div class="approvals__tips">
        <h3 class="approvals__tips-title">Mejores prácticas</h3>
        <div class="approvals__tips-grid">
          <div class="approvals__tip">
            <span class="tip-icon">✂️</span>
            <div>
              <strong>Revoca después de cada interacción</strong>
              <p>Una vez que termines con una dApp, revoca su aprobación. No hay desventaja — puedes volver a aprobarla la próxima vez que la uses.</p>
            </div>
          </div>
          <div class="approvals__tip">
            <span class="tip-icon">🔢</span>
            <div>
              <strong>Establece montos exactos, no ilimitados</strong>
              <p>Al aprobar un swap, algunas billeteras te permiten establecer un monto personalizado. Aprueba siempre solo lo que necesites para esa transacción.</p>
            </div>
          </div>
          <div class="approvals__tip">
            <span class="tip-icon">🗓️</span>
            <div>
              <strong>Audita tus aprobaciones regularmente</strong>
              <p>Haz una verificación en revoke.cash cada pocos meses — especialmente después de noticias de un exploit DeFi, ya que los atacantes a menudo apuntan a aprobaciones antiguas.</p>
            </div>
          </div>
          <div class="approvals__tip">
            <span class="tip-icon">🦊</span>
            <div>
              <strong>Lee lo que MetaMask realmente te pide</strong>
              <p>Antes de hacer clic en Confirmar, expande los detalles de la transacción. Si dice "Ilimitado" junto a una cantidad de token — esa es una señal de alerta que vale la pena pausar.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Preguntas frecuentes -->
    <section class="faq" aria-labelledby="faq-heading">
      <h2 id="faq-heading" class="faq__title">Preguntas frecuentes y patrones de estafa</h2>
      <div class="faq__list">
        {[
          {
            q: '¿Qué es el verificador de dApp / sitio web?',
            a: 'Es una herramienta gratuita que toma cualquier URL o dominio y consulta hasta 7 bases de datos de seguridad independientes simultáneamente — incluyendo la propia lista de bloqueo de phishing de MetaMask, ScamSniffer, GoPlus, URLScan.io y OpenPhish. Devuelve un resultado rojo, amarillo o verde según lo que reportan esas bases de datos. No hacemos nuestra propia determinación — mostramos lo que la comunidad de seguridad ya ha señalado.',
          },
          {
            q: '¿Qué significa un resultado rojo para un sitio web?',
            a: 'Significa que una o más de las bases de datos de seguridad que consultamos ha reportado ese dominio. No significa que nosotros lo estemos llamando una estafa — esa determinación proviene de la base de datos de terceros. Debes tratar un resultado rojo como una advertencia seria, hacer tu propia investigación adicional y no conectar tu billetera hasta estar seguro de que el sitio es legítimo.',
          },
          {
            q: '¿Qué significa un resultado amarillo?',
            a: 'Amarillo significa que el sitio no está en ninguna lista de bloqueo, pero tampoco tiene poco o ningún historial de escaneos de seguridad — por lo que no hay suficientes datos para dar un certificado de salud limpio. Los sitios nuevos, dominios oscuros o direcciones registradas recientemente a menudo muestran amarillo. Procede con precaución y verifica el sitio a través de canales oficiales antes de conectarte.',
          },
          {
            q: '¿Puedo confiar en un sitio solo porque muestra verde?',
            a: 'No. Un resultado verde significa que el sitio no ha sido reportado a ninguna de las bases de datos que verificamos — no que sea definitivamente seguro. Los sitios de phishing nuevos tienen unas pocas horas antes de ser añadidos a las listas de bloqueo. Siempre verifica la URL exacta en la barra de tu navegador, busca las cuentas oficiales de redes sociales y nunca conectes una billetera desde un enlace enviado en un mensaje directo o correo electrónico.',
          },
          {
            q: '¿Cómo funcionan los sitios de wallet drainer?',
            a: 'Un wallet drainer es un sitio web que imita una dApp legítima — un mint falso de NFT, una reclamación falsa de token o un airdrop falso. Cuando conectas tu MetaMask y firmas una transacción, en realidad estás firmando un permiso que le permite al atacante transferir todos los tokens de tu billetera en un solo movimiento. Todo el saldo puede desaparecer en segundos. El sitio a menudo desaparece en pocas horas.',
          },
          {
            q: '¿Qué es una estafa honeypot?',
            a: 'Un honeypot es un token que puedes comprar pero nunca vender. El estafador lo promociona, compras, el precio parece subir — pero cuando intentas vender, el contrato te bloquea. El estafador luego drena la liquidez y desaparece con tu ETH.',
          },
          {
            q: '¿Cómo se ve "demasiado bueno para ser verdad" en cripto?',
            a: 'Rendimientos diarios garantizados del 1–10%, "solo pon tus tokens en nuestra billetera", reclamaciones de airdrop que requieren enviar tokens primero, o alguien en mensajes directos ofreciendo duplicar tu cripto. Si el retorno suena imposible en las finanzas tradicionales, es una estafa en cripto.',
          },
          {
            q: '¿Por qué usaría una billetera Tornado Cash?',
            a: 'Tornado Cash es un mixer que rompe el enlace on-chain entre direcciones de billetera. Aunque algunos usuarios valoran la privacidad, es ampliamente utilizado por estafadores y hackers para ocultar el origen de fondos robados antes de cobrarlos.',
          },
          {
            q: '¿Debo confiar en una billetera solo porque tiene un saldo grande?',
            a: 'No. Los estafadores a menudo cargan billeteras con tokens sin valor o saldos "de papel" inflados para crear apariencia de legitimidad. Verifica siempre si esos tokens pueden realmente venderse y cuánto valen realmente.',
          },
          {
            q: '¿Es una billetera nueva siempre sospechosa?',
            a: 'No siempre — pero en el contexto de alguien que ofrece una inversión, una billetera creada en los últimos 30 días es una gran señal de alerta. Los protocolos y negocios legítimos tienen historial on-chain establecido.',
          },
          {
            q: '¿Qué debo hacer si esta herramienta señala una dirección de billetera?',
            a: 'No envíes fondos. Toma una captura de pantalla de los resultados. Si alguien te presiona para enviar cripto a una dirección señalada, esa presión misma es parte de la estafa. Reporta la dirección en chainabuse.com y retírate.',
          },
          {
            q: '¿Qué debo hacer si el verificador de dApp señala un sitio web?',
            a: 'No conectes tu billetera. Cierra la pestaña. Encuentra el proyecto oficial a través de una fuente de confianza — su cuenta verificada de Twitter/X o un agregador reconocido como DeFiLlama o CoinGecko. Reporta el sitio a la base de datos de phishing de MetaMask en github.com/MetaMask/eth-phishing-detect.',
          },
        ].map(item => (
          <details class="faq__item">
            <summary class="faq__q">
              <span>{item.q}</span>
              <span class="faq__chevron" aria-hidden="true">›</span>
            </summary>
            <p class="faq__a">{item.a}</p>
          </details>
        ))}
      </div>
    </section>

    <!-- Aviso legal -->
    <p class="disclaimer">
      Los resultados de verificación de billeteras provienen de bases de datos públicas incluyendo GoPlus Security, Etherscan, Alchemy y honeypot.is.
      Los resultados de dApp / sitio web provienen de MetaMask eth-phishing-detect, ScamSniffer, GoPlus Security, URLScan.io y OpenPhish.
      Todos los hallazgos se reportan desde bases de datos de terceros y no son verificados independientemente por Almstins.
      Esta herramienta no constituye asesoramiento financiero ni legal. Haz siempre tu propia investigación.
      <br />
      <a class="disclaimer__link" href="/">Una herramienta gratuita de Almstins — rastreador de portafolio cripto y herramienta de contabilidad.</a>
    </p>

  </div>
</LoginLayout>

<style>
  /* ── Sección de aprobaciones de billetera ── */
  .approvals-section {
    max-width: 860px;
    margin: 0 auto 4rem;
    padding: 0 1.5rem;
  }
  .approvals__title {
    font-size: clamp(1.3rem, 2.5vw, 1.75rem);
    font-weight: 800; letter-spacing: -0.01em;
    margin: 0 0 0.85rem; color: #f5f8ff;
  }
  .approvals__intro {
    font-size: 0.97rem; color: rgba(255,255,255,0.55);
    line-height: 1.75; margin: 0 0 1.75rem;
    max-width: 740px;
  }

  .approvals__cards {
    display: grid; grid-template-columns: 1fr 1fr; gap: 1rem;
    margin-bottom: 1.75rem;
  }
  @media (max-width: 680px) { .approvals__cards { grid-template-columns: 1fr; } }

  .approvals__card {
    border-radius: 14px; padding: 1.5rem;
    border: 1px solid rgba(255,255,255,0.08);
    background: rgba(255,255,255,0.03);
  }
  .approvals__card--what { border-color: rgba(248,113,113,0.2); background: rgba(248,113,113,0.04); }
  .approvals__card--how  { border-color: rgba(99,102,241,0.2);  background: rgba(99,102,241,0.04); }

  .approvals__card h3 {
    font-size: 0.95rem; font-weight: 700; margin: 0 0 0.75rem; color: #f5f8ff;
  }
  .approvals__card p {
    font-size: 0.85rem; color: rgba(255,255,255,0.55); line-height: 1.7; margin: 0 0 0.65rem;
  }
  .approvals__card p:last-child { margin-bottom: 0; }
  .approvals__card em  { color: rgba(255,255,255,0.75); font-style: normal; }
  .approvals__card strong { color: rgba(255,255,255,0.9); }

  /* Lista de herramientas */
  .approvals__tools {
    list-style: none; padding: 0; margin: 0;
    display: flex; flex-direction: column; gap: 0.85rem;
  }
  .approvals__tools li { display: flex; flex-direction: column; gap: 0.2rem; }
  .tool-link {
    display: inline-flex; align-items: center; gap: 0.5rem;
    text-decoration: none; color: #a5b4fc; font-weight: 700; font-size: 0.9rem;
    transition: color 0.15s;
  }
  .tool-link:hover { color: #c7d2fe; }
  .tool-link__name { text-decoration: underline; text-underline-offset: 3px; }
  .tool-link__badge {
    font-size: 0.65rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.07em;
    background: rgba(74,222,128,0.15); color: #4ade80;
    border: 1px solid rgba(74,222,128,0.25); border-radius: 99px;
    padding: 0.1rem 0.45rem;
  }
  .tool-link__desc { font-size: 0.78rem; color: rgba(255,255,255,0.4); line-height: 1.5; }

  /* Cuadrícula de consejos */
  .approvals__tips {
    background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.07);
    border-radius: 14px; padding: 1.5rem;
  }
  .approvals__tips-title {
    font-size: 0.9rem; font-weight: 700; color: rgba(255,255,255,0.6);
    text-transform: uppercase; letter-spacing: 0.07em;
    margin: 0 0 1.1rem;
  }
  .approvals__tips-grid {
    display: grid; grid-template-columns: 1fr 1fr; gap: 1rem;
  }
  @media (max-width: 560px) { .approvals__tips-grid { grid-template-columns: 1fr; } }

  .approvals__tip {
    display: flex; align-items: flex-start; gap: 0.7rem;
  }
  .tip-icon { font-size: 1.2rem; flex-shrink: 0; margin-top: 0.1rem; }
  .approvals__tip strong { display: block; font-size: 0.85rem; color: #f5f8ff; margin-bottom: 0.25rem; }
  .approvals__tip p { font-size: 0.78rem; color: rgba(255,255,255,0.4); line-height: 1.6; margin: 0; }

  /* ── Barra de navegación superior ── */
  .topnav {
    position: sticky;
    top: 0;
    z-index: 50;
    width: 100%;
    box-sizing: border-box;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.85rem 1.25rem;
    background: rgba(5, 7, 15, 0.88);
    backdrop-filter: blur(14px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
  }

  .topnav__brand {
    display: flex;
    align-items: baseline;
    gap: 0.6rem;
    text-decoration: none;
  }

  .topnav__wordmark {
    font-size: 1.05rem;
    font-weight: 800;
    background: linear-gradient(135deg, #f5f8ff 30%, #a5b4fc);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    letter-spacing: -0.02em;
  }

  .topnav__tagline {
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.35);
    display: none;
  }

  @media (min-width: 560px) {
    .topnav__tagline { display: inline; }
  }

  .topnav__actions {
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }

  .topnav__lang {
    font-size: 0.875rem;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.5);
    text-decoration: none;
    padding: 0.4rem 0.75rem;
    border-radius: 999px;
    transition: color 0.15s ease;
    margin-right: 0.25rem;
  }
  .topnav__lang:hover {
    color: #f5f8ff;
  }

  .topnav__login {
    font-size: 0.875rem;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.6);
    text-decoration: none;
    padding: 0.4rem 0.75rem;
    border-radius: 999px;
    transition: color 0.15s ease;
  }

  .topnav__login:hover {
    color: #f5f8ff;
  }

  .topnav__signup {
    font-size: 0.875rem;
    font-weight: 600;
    color: #fff;
    text-decoration: none;
    padding: 0.45rem 1.1rem;
    border-radius: 999px;
    background: linear-gradient(135deg, #5767ff, #934dff);
    transition: filter 0.15s ease, transform 0.15s ease;
    white-space: nowrap;
  }

  .topnav__signup:hover {
    filter: brightness(1.12);
    transform: translateY(-1px);
  }

  /* ── Página ── */
  .page {
    width: 100%;
    box-sizing: border-box;
    max-width: 860px;
    margin: 0 auto;
    padding: 3rem 1.25rem 6rem;
    color: #f5f8ff;
    font-family: 'Inter', system-ui, sans-serif;
  }

  @media (max-width: 480px) {
    .page { padding: 2rem 1rem 4rem; }
    .topnav { padding: 0.75rem 1rem; }
    .topnav__login { display: none; }
  }

  /* Hero */
  .hero {
    text-align: center;
    padding: 2rem 0 2.5rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
    margin-bottom: 2.5rem;
  }

  .hero__eyebrow {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: #a5b4fc;
    background: rgba(87, 103, 255, 0.12);
    border: 1px solid rgba(87, 103, 255, 0.25);
    padding: 0.3rem 0.9rem;
    border-radius: 999px;
    margin-bottom: 1.25rem;
  }

  .hero__title {
    font-size: clamp(2.2rem, 5vw, 3.6rem);
    font-weight: 800;
    line-height: 1.2;
    margin: 0 0 1.1rem;
    background: linear-gradient(135deg, #f5f8ff 30%, #a5b4fc);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero__sub {
    font-size: 1.2rem;
    color: rgba(255, 255, 255, 0.6);
    line-height: 1.9;
    max-width: 640px;
    margin: 0 auto 2rem;
  }

  /* Cuadrícula de amenazas */
  .threat-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
    max-width: 980px;
    margin: 0 auto 2.5rem;
  }

  .threat-card {
    display: flex;
    flex-direction: column;
    background: rgba(255, 255, 255, 0.04);
    border: 2px solid rgba(165, 180, 252, 0.4);
    border-radius: 16px;
    overflow: hidden;
    transition: border-color 0.15s, box-shadow 0.15s;
  }

  .threat-card:hover {
    border-color: rgba(165, 180, 252, 0.75);
    box-shadow: 0 0 0 3px rgba(165, 180, 252, 0.1);
  }

  /* Mitad superior — clic para modal */
  .threat-card__top {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    padding: 1.75rem 1.5rem 1.5rem;
    background: none;
    border: none;
    color: inherit;
    font-family: inherit;
    text-align: center;
    cursor: pointer;
    width: 100%;
    transition: background 0.15s;
  }

  .threat-card__top:hover {
    background: rgba(165, 180, 252, 0.08);
  }

  .threat-card__icon {
    font-size: 2.8rem;
    line-height: 1;
  }

  .threat-card__title {
    font-size: 1.1rem;
    font-weight: 700;
    color: #e2e8ff;
    line-height: 1.4;
  }

  .threat-card__cta {
    font-size: 0.85rem;
    color: #a5b4fc;
  }

  /* Divisor */
  .threat-card__divider {
    height: 1px;
    background: rgba(165, 180, 252, 0.25);
    margin: 0;
  }

  /* Mitad inferior — área de entrada */
  .threat-card__checker {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    padding: 1.25rem 1.25rem 1.5rem;
    background: rgba(0, 0, 0, 0.15);
  }

  .threat-card__input {
    width: 100%;
    padding: 0.6rem 0.85rem;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 8px;
    color: #e2e8ff;
    font-size: 0.9rem;
    font-family: inherit;
    outline: none;
    transition: border-color 0.15s;
    box-sizing: border-box;
  }

  .threat-card__input::placeholder {
    color: rgba(255, 255, 255, 0.3);
  }

  .threat-card__input:focus {
    border-color: rgba(165, 180, 252, 0.6);
    background: rgba(255, 255, 255, 0.09);
  }

  .threat-card__check-btn {
    width: 100%;
    padding: 0.65rem 1rem;
    background: rgba(87, 103, 255, 0.7);
    border: 1px solid rgba(165, 180, 252, 0.4);
    border-radius: 8px;
    color: #fff;
    font-size: 0.95rem;
    font-weight: 600;
    font-family: inherit;
    cursor: pointer;
    transition: background 0.15s;
  }

  .threat-card__check-btn:hover {
    background: rgba(87, 103, 255, 0.9);
  }

  /* Enlaces de aprobación (mitad inferior de tarjeta 3) */
  .threat-card__approval-links {
    display: flex;
    flex-direction: column;
    padding: 1.1rem 1.25rem 1.4rem;
    background: rgba(0, 0, 0, 0.15);
    gap: 0;
  }

  .threat-card__approval-desc {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.5);
    line-height: 1.6;
    margin: 0 0 0.85rem;
  }

  .threat-card__approval-link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.7rem 0.85rem;
    border-radius: 8px;
    text-decoration: none;
    transition: background 0.15s;
    margin-bottom: 0.3rem;
    border: 1px solid transparent;
  }

  .threat-card__approval-link:last-child {
    margin-bottom: 0;
  }

  .threat-card__approval-link:hover {
    background: rgba(165, 180, 252, 0.1);
    border-color: rgba(165, 180, 252, 0.25);
  }

  .approval-link__name {
    font-size: 1rem;
    font-weight: 600;
    color: #a5b4fc;
  }

  .approval-link__arrow {
    font-size: 0.9rem;
    color: rgba(165, 180, 252, 0.6);
  }

  /* Modales de amenaza */
  .threat-modal {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 1000;
    align-items: center;
    justify-content: center;
    padding: 1rem;
    background: rgba(0, 0, 0, 0.65);
    backdrop-filter: blur(4px);
  }

  .threat-modal.open {
    display: flex;
  }

  .threat-modal__box {
    background: #1a1d2e;
    border: 1px solid rgba(165, 180, 252, 0.2);
    border-radius: 16px;
    padding: 2rem;
    max-width: 560px;
    width: 100%;
    max-height: 90vh;
    overflow-y: auto;
    position: relative;
    box-shadow: 0 24px 64px rgba(0, 0, 0, 0.6);
  }

  .threat-modal__close {
    position: absolute;
    top: 1rem;
    right: 1rem;
    background: none;
    border: none;
    color: rgba(255,255,255,0.45);
    font-size: 1.4rem;
    cursor: pointer;
    line-height: 1;
    padding: 0.2rem 0.5rem;
    border-radius: 6px;
    transition: color 0.15s, background 0.15s;
  }

  .threat-modal__close:hover {
    color: #fff;
    background: rgba(255,255,255,0.08);
  }

  .threat-modal__icon {
    font-size: 2.5rem;
    margin-bottom: 0.75rem;
  }

  .threat-modal__title {
    font-size: 1.25rem;
    font-weight: 700;
    color: #e2e8ff;
    margin: 0 0 0.5rem;
  }

  .threat-modal__subtitle {
    font-size: 0.85rem;
    color: #f87171;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin: 0 0 1.25rem;
  }

  .threat-modal__body {
    font-size: 0.95rem;
    color: rgba(255,255,255,0.7);
    line-height: 1.75;
  }

  .threat-modal__body p { margin: 0 0 0.9rem; }
  .threat-modal__body p:last-child { margin-bottom: 0; }

  .threat-modal__example {
    background: rgba(248, 113, 113, 0.08);
    border-left: 3px solid #f87171;
    border-radius: 4px;
    padding: 0.75rem 1rem;
    margin: 1rem 0;
    font-size: 0.88rem;
    color: rgba(255,255,255,0.65);
  }

  .threat-modal__tip {
    background: rgba(52, 211, 153, 0.08);
    border-left: 3px solid #34d399;
    border-radius: 4px;
    padding: 0.75rem 1rem;
    margin: 1rem 0;
    font-size: 0.88rem;
    color: rgba(255,255,255,0.65);
  }

  @media (max-width: 640px) {
    .threat-grid { grid-template-columns: 1fr; }
  }

  /* Verificador */
  .checker-wrap {
    margin-bottom: 2.5rem;
  }

  /* ── Banner CTA de Almstins ── */
  .cta-banner {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    background: linear-gradient(135deg, rgba(87, 103, 255, 0.12), rgba(147, 77, 255, 0.1));
    border: 1px solid rgba(87, 103, 255, 0.28);
    border-radius: 16px;
    padding: 1.4rem 1.6rem;
    margin-bottom: 4rem;
    flex-wrap: wrap;
  }

  .cta-banner__icon {
    font-size: 2rem;
    flex-shrink: 0;
    line-height: 1;
  }

  .cta-banner__body {
    flex: 1;
    min-width: 200px;
  }

  .cta-banner__headline {
    margin: 0 0 0.3rem;
    font-size: 1rem;
    font-weight: 700;
    color: #f5f8ff;
  }

  .cta-banner__sub {
    margin: 0;
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.5);
    line-height: 1.6;
  }

  .cta-banner__btn {
    flex-shrink: 0;
    display: inline-block;
    padding: 0.65rem 1.3rem;
    border-radius: 999px;
    background: linear-gradient(135deg, #5767ff, #934dff);
    color: #fff;
    font-size: 0.9rem;
    font-weight: 600;
    text-decoration: none;
    white-space: nowrap;
    transition: filter 0.15s ease, transform 0.15s ease;
  }

  .cta-banner__btn:hover {
    filter: brightness(1.12);
    transform: translateY(-1px);
  }

  /* Cuadrícula de "qué verificamos" */
  .signals {
    margin-bottom: 4rem;
  }

  .signals__title {
    font-size: 1.3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: rgba(255, 255, 255, 0.85);
  }

  .signals__sub {
    font-size: 0.78rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: rgba(255,255,255,0.4);
    margin: 0 0 0.85rem;
  }

  .signals__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
  }

  .signal-card {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 12px;
    padding: 1.1rem 1.25rem;
  }

  .signal-card__icon {
    font-size: 1.3rem;
    display: block;
    margin-bottom: 0.5rem;
  }

  .signal-card__label {
    font-size: 0.875rem;
    font-weight: 700;
    color: rgba(255, 255, 255, 0.85);
    margin: 0 0 0.4rem;
  }

  .signal-card__body {
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.4);
    line-height: 1.65;
    margin: 0;
  }

  /* Preguntas frecuentes */
  .faq {
    margin-bottom: 3rem;
  }

  .faq__title {
    font-size: 1.3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: rgba(255, 255, 255, 0.85);
  }

  .faq__item {
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
    padding: 0.25rem 0;
  }

  .faq__item[open] {
    padding-bottom: 0.75rem;
  }

  .faq__q {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    padding: 0.9rem 0.25rem;
    font-size: 0.95rem;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.8);
    cursor: pointer;
    list-style: none;
    user-select: none;
  }

  .faq__q::-webkit-details-marker { display: none; }
  .faq__q::marker { display: none; }

  .faq__chevron {
    font-size: 1.3rem;
    opacity: 0.4;
    transition: transform 0.2s ease;
    flex-shrink: 0;
  }

  .faq__item[open] .faq__chevron {
    transform: rotate(90deg);
  }

  .faq__a {
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.5);
    line-height: 1.75;
    margin: 0 0 0 0.25rem;
    padding-right: 2rem;
  }

  /* Aviso legal */
  .disclaimer {
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.25);
    line-height: 1.7;
    text-align: center;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
    padding-top: 2rem;
  }

  .disclaimer__link {
    color: rgba(165, 180, 252, 0.5);
    text-decoration: none;
    display: inline-block;
    margin-top: 0.4rem;
    transition: color 0.15s ease;
  }

  .disclaimer__link:hover {
    color: #a5b4fc;
  }

  /* ── Pestañas del verificador ── */
  .checker-tabs {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 1.25rem;
    border-bottom: 1px solid rgba(255,255,255,0.08);
    padding-bottom: 0;
  }

  .checker-tab {
    padding: 0.65rem 1.2rem;
    font-size: 0.9rem;
    font-weight: 600;
    color: rgba(255,255,255,0.45);
    background: none;
    border: none;
    border-bottom: 2px solid transparent;
    cursor: pointer;
    transition: color 0.15s, border-color 0.15s;
    margin-bottom: -1px;
  }

  .checker-tab:hover { color: rgba(255,255,255,0.75); }

  .checker-tab.active {
    color: #a5b4fc;
    border-bottom-color: #a5b4fc;
  }

  /* ── Panel dApp ── */
  .dapp-panel {
    margin-bottom: 2.5rem;
  }

  .dapp-input-row {
    display: flex;
    gap: 0.65rem;
    margin-bottom: 1rem;
  }

  .dapp-input-row input {
    flex: 1;
    padding: 0.8rem 1rem;
    border-radius: 12px;
    border: 1px solid rgba(255,255,255,0.15);
    background: rgba(255,255,255,0.06);
    color: #f5f8ff;
    font-size: 0.95rem;
    font-family: inherit;
    outline: none;
    transition: border-color 0.15s;
  }

  .dapp-input-row input:focus { border-color: rgba(165,180,252,0.6); }
  .dapp-input-row input::placeholder { color: rgba(255,255,255,0.3); }

  .dapp-input-row button {
    padding: 0.8rem 1.4rem;
    border-radius: 12px;
    border: none;
    background: linear-gradient(135deg, #5767ff, #934dff);
    color: #fff;
    font-size: 0.9rem;
    font-weight: 700;
    cursor: pointer;
    white-space: nowrap;
    transition: filter 0.15s, transform 0.15s;
  }

  .dapp-input-row button:hover { filter: brightness(1.12); transform: translateY(-1px); }
  .dapp-input-row button:disabled { opacity: 0.5; cursor: not-allowed; transform: none; }

  /* ── Cargando ── */
  .dapp-loading {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem;
    font-size: 0.9rem;
    color: rgba(255,255,255,0.5);
  }

  .spinner {
    display: inline-block;
    width: 18px;
    height: 18px;
    border: 2px solid rgba(165,180,252,0.25);
    border-top-color: #a5b4fc;
    border-radius: 50%;
    animation: spin 0.7s linear infinite;
  }

  @keyframes spin { to { transform: rotate(360deg); } }

  /* ── Error ── */
  .dapp-error {
    padding: 0.85rem 1rem;
    border-radius: 12px;
    background: rgba(255,107,107,0.08);
    border: 1px solid rgba(255,107,107,0.3);
    color: #ff9b9b;
    font-size: 0.9rem;
  }

  /* ── Banner de veredicto ── */
  .verdict-banner {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.2rem 1.4rem;
    border-radius: 16px;
    margin-bottom: 1rem;
  }

  .verdict-banner.red    { background: rgba(255,59,59,0.12);  border: 1px solid rgba(255,59,59,0.35); }
  .verdict-banner.yellow { background: rgba(255,193,7,0.1);   border: 1px solid rgba(255,193,7,0.3);  }
  .verdict-banner.green  { background: rgba(52,211,153,0.1);  border: 1px solid rgba(52,211,153,0.3); }

  .verdict-icon { font-size: 2.2rem; line-height: 1; }

  .verdict-label {
    margin: 0 0 0.25rem;
    font-size: 1.1rem;
    font-weight: 800;
  }

  .verdict-banner.red    .verdict-label { color: #ff6b6b; }
  .verdict-banner.yellow .verdict-label { color: #ffc107; }
  .verdict-banner.green  .verdict-label { color: #34d399; }

  .verdict-url {
    margin: 0;
    font-size: 0.8rem;
    color: rgba(255,255,255,0.4);
    word-break: break-all;
  }

  /* ── Lista de fuentes ── */
  .source-list {
    list-style: none;
    padding: 0;
    margin: 0 0 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .source-item {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    border-radius: 12px;
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.07);
    font-size: 0.875rem;
  }

  .source-item.flagged { border-color: rgba(255,59,59,0.35);   background: rgba(255,59,59,0.07);  }
  .source-item.clean   { border-color: rgba(52,211,153,0.2);   }
  .source-item.yellow  { border-color: rgba(255,193,7,0.25);   background: rgba(255,193,7,0.05);  }

  .source-badge {
    font-size: 1rem;
    flex-shrink: 0;
    line-height: 1.4;
  }

  .source-name {
    font-weight: 700;
    color: rgba(255,255,255,0.85);
    margin: 0 0 0.15rem;
  }

  .source-detail {
    margin: 0;
    color: rgba(255,255,255,0.45);
    font-size: 0.8rem;
    line-height: 1.5;
  }

  .source-verdict-dot {
    margin-left: auto;
    flex-shrink: 0;
    width: 10px;
    height: 10px;
    border-radius: 50%;
    align-self: center;
  }

  .dot-red    { background: #ff4d4d; box-shadow: 0 0 8px rgba(255,77,77,0.6); }
  .dot-green  { background: #34d399; box-shadow: 0 0 8px rgba(52,211,153,0.4); }
  .dot-yellow { background: #ffc107; box-shadow: 0 0 8px rgba(255,193,7,0.4); }
  .dot-gray   { background: rgba(255,255,255,0.2); }

  /* ── Consejo de seguridad ── */
  .safety-tip {
    padding: 0.9rem 1.1rem;
    border-radius: 12px;
    font-size: 0.85rem;
    line-height: 1.65;
    color: rgba(255,255,255,0.55);
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.07);
  }

  /* ── Responsive ── */
  @media (max-width: 600px) {
    .signals__grid      { grid-template-columns: 1fr; }
    .hero__title        { -webkit-text-fill-color: #f5f8ff; }
    .cta-banner         { flex-direction: column; text-align: center; }
    .cta-banner__icon   { align-self: center; }
    .cta-banner__btn    { align-self: stretch; text-align: center; }
    .dapp-input-row     { flex-direction: column; }
  }
</style>

<script>
  // ── Modales de amenaza ─────────────────────────────────────────────────────────
  function openModal(id: string) {
    const modal = document.getElementById(id);
    if (!modal) return;
    modal.classList.add('open');
    document.body.style.overflow = 'hidden';
    const closeBtn = modal.querySelector<HTMLButtonElement>('.threat-modal__close');
    closeBtn?.focus();
  }

  function closeModal(modal: HTMLElement) {
    modal.classList.remove('open');
    document.body.style.overflow = '';
  }

  // Mitad superior de cada tarjeta → abrir modal
  document.querySelectorAll<HTMLButtonElement>('.threat-card__top').forEach((btn) => {
    btn.addEventListener('click', () => {
      const modalId = btn.dataset.modal;
      if (modalId) openModal(modalId);
    });
  });

  // Botones de verificación de la mitad inferior
  document.querySelectorAll<HTMLButtonElement>('.threat-card__check-btn').forEach((btn) => {
    btn.addEventListener('click', (e) => {
      e.stopPropagation();
      const type  = btn.dataset.checker;
      const input = btn.closest('.threat-card__checker')
                       ?.querySelector<HTMLInputElement>('.threat-card__input');
      const value = input?.value.trim() ?? '';
      if (!value) { input?.focus(); return; }

      if (type === 'dapp') {
        // Cambiar a pestaña dApp, pre-rellenar y ejecutar verificación
        const dappTab   = document.getElementById('tab-btn-dapp') as HTMLButtonElement | null;
        const walletTab = document.getElementById('tab-btn-wallet') as HTMLButtonElement | null;
        const dappPanel   = document.getElementById('panel-dapp')   as HTMLElement | null;
        const walletPanel = document.getElementById('panel-wallet') as HTMLElement | null;
        const urlInput  = document.getElementById('dapp-url-input') as HTMLInputElement | null;
        const checkBtn  = document.getElementById('dapp-check-btn') as HTMLButtonElement | null;
        if (dappTab && walletTab && dappPanel && walletPanel && urlInput && checkBtn) {
          walletTab.classList.remove('active'); walletTab.setAttribute('aria-selected', 'false');
          dappTab.classList.add('active');      dappTab.setAttribute('aria-selected', 'true');
          walletPanel.hidden = true;
          dappPanel.hidden   = false;
          urlInput.value = value;
          dappPanel.scrollIntoView({ behavior: 'smooth', block: 'start' });
          setTimeout(() => checkBtn.click(), 350);
        }
      } else if (type === 'wallet') {
        // Cambiar a pestaña de billetera y navegar a ?address= para que React pre-rellene
        const url = new URL(window.location.href);
        url.searchParams.set('address', value);
        url.hash = 'panel-wallet';
        window.location.href = url.toString();
      } else if (type === 'approval') {
        // Abrir revoke.cash con la dirección
        const addr = encodeURIComponent(value);
        window.open(`https://revoke.cash/address/${addr}`, '_blank', 'noopener,noreferrer');
      }
    });
  });

  // Evitar que los clics en el input se propaguen a la parte superior de la tarjeta
  document.querySelectorAll<HTMLInputElement>('.threat-card__input').forEach((inp) => {
    inp.addEventListener('click', (e) => e.stopPropagation());
    inp.addEventListener('keydown', (e) => {
      if (e.key === 'Enter') {
        e.preventDefault();
        (inp.closest('.threat-card__checker')
             ?.querySelector<HTMLButtonElement>('.threat-card__check-btn'))?.click();
      }
    });
  });

  document.querySelectorAll<HTMLElement>('.threat-modal').forEach((modal) => {
    // Botón de cerrar
    modal.querySelector('.threat-modal__close')?.addEventListener('click', () => closeModal(modal));
    // Clic en el fondo para cerrar
    modal.addEventListener('click', (e) => {
      if (e.target === modal) closeModal(modal);
    });
  });

  // La tecla Escape cierra cualquier modal abierto
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      document.querySelectorAll<HTMLElement>('.threat-modal.open').forEach(closeModal);
    }
  });

  // ── Cambio de pestañas ────────────────────────────────────────────────────────
  const tabs = document.querySelectorAll<HTMLButtonElement>('.checker-tab');
  tabs.forEach((btn) => {
    btn.addEventListener('click', () => {
      tabs.forEach((t) => { t.classList.remove('active'); t.setAttribute('aria-selected', 'false'); });
      btn.classList.add('active');
      btn.setAttribute('aria-selected', 'true');

      const target = btn.dataset.tab!;
      (document.getElementById('panel-wallet') as HTMLElement).hidden = target !== 'wallet';
      (document.getElementById('panel-dapp')   as HTMLElement).hidden = target !== 'dapp';
    });
  });

  // ── Verificador de dApp ───────────────────────────────────────────────────────
  const input   = document.getElementById('dapp-url-input')   as HTMLInputElement;
  const btn     = document.getElementById('dapp-check-btn')   as HTMLButtonElement;
  const result  = document.getElementById('dapp-result')      as HTMLElement;
  const loading = document.getElementById('dapp-loading')     as HTMLElement;
  const errBox  = document.getElementById('dapp-error')       as HTMLElement;

  function setLoading(on: boolean) {
    btn.disabled   = on;
    loading.hidden = !on;
    result.hidden  = true;
    errBox.hidden  = true;
  }

  function verdictDot(v: string): string {
    if (v === 'flagged')   return 'dot-red';
    if (v === 'clean' || v === 'whitelisted') return 'dot-green';
    if (v === 'unscanned') return 'dot-yellow';
    return 'dot-gray';
  }

  function sourceClass(v: string): string {
    if (v === 'flagged')   return 'flagged';
    if (v === 'unscanned') return 'yellow';
    return 'clean';
  }

  function verdictEmoji(v: string): string {
    if (v === 'flagged') return '🚫';
    if (v === 'clean' || v === 'whitelisted') return '✅';
    return '⚠️';
  }

  async function runCheck() {
    const raw = input.value.trim();
    if (!raw) { input.focus(); return; }

    setLoading(true);
    try {
      const qs  = new URLSearchParams({ url: raw });
      const res = await fetch(`/api/dapp-check?${qs}`);
      const data = await res.json();

      if (!res.ok || data.error) throw new Error(data.message ?? `HTTP ${res.status}`);

      // Banner de veredicto
      const banner = document.getElementById('verdict-banner')!;
      banner.className = `verdict-banner ${data.verdict}`;
      document.getElementById('verdict-icon')!.textContent  =
        data.verdict === 'red' ? '🚨' : data.verdict === 'yellow' ? '⚠️' : '✅';
      document.getElementById('verdict-label')!.textContent =
        data.verdict === 'red'    ? 'PELIGRO — No conectes tu billetera' :
        data.verdict === 'yellow' ? 'PRECAUCIÓN — Procede con cuidado' :
                                    'PARECE SEGURO — No se detectaron amenazas';
      document.getElementById('verdict-url')!.textContent = data.domain;

      // Lista de fuentes
      const list = document.getElementById('source-list')!;
      list.innerHTML = (data.sources as any[]).map((s) => `
        <li class="source-item ${sourceClass(s.verdict)}">
          <span class="source-badge">${s.icon}</span>
          <div>
            <p class="source-name">${s.name}</p>
            <p class="source-detail">${s.detail}</p>
          </div>
          <span class="source-verdict-dot ${verdictDot(s.verdict)}"></span>
        </li>`).join('');

      // Consejo de seguridad
      const tip = document.getElementById('safety-tip')!;
      tip.textContent =
        data.verdict === 'red'
          ? '🛑 Una o más bases de datos de seguridad han reportado este sitio. Nosotros no estamos haciendo esa determinación — verifica siempre de forma independiente. NO conectes tu billetera ni firmes ninguna transacción hasta estar seguro.'
          : data.verdict === 'yellow'
          ? '⚠️ Este sitio tiene historial de escaneo limitado. Solo conecta tu billetera a sitios que encontraste tú mismo — nunca a través de un enlace en un mensaje directo o correo electrónico.'
          : '✅ No se detectaron amenazas en todas las fuentes. Siempre verifica la URL en la barra de tu navegador antes de firmar cualquier transacción — incluso los sitios de apariencia segura pueden ser typosquatted.';

      result.hidden = false;
      if (typeof (window as any).gtag === 'function') {
        (window as any).gtag('event', 'dapp_check_submitted', {
          event_category: 'interactive_tool',
          event_label: data.verdict ?? 'unknown',
        });
      }
    } catch (e) {
      errBox.textContent = `Verificación fallida: ${e instanceof Error ? e.message : 'Error desconocido'}. Inténtalo de nuevo.`;
      errBox.hidden = false;
    } finally {
      setLoading(false);
    }
  }

  btn.addEventListener('click', runCheck);
  input.addEventListener('keydown', (e) => { if (e.key === 'Enter') runCheck(); });
</script>
